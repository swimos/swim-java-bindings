steps:
  - script: |
      rustup toolchain install nightly --component miri
      rustup override set nightly

      cargo miri test
    displayName: Miri
    workingDirectory: $(Build.SourcesDirectory)/rust-java-bridge